{
  "buildCommand": "pnpm build && cp -r dist/client/* public/",
  "functions": {
    "api/[[...path]].ts": {
      "includeFiles": "dist/server/**/*"
    }
  },
  "rewrites": [
    { "source": "/((?!assets|favicon|styles).*)", "destination": "/api/[[...path]]" }
  ]
}
